import { SortArrayByUpdatedOnPipe } from './sort-array-by-updated-on.pipe';

describe('SortArrayByUpdatedOnPipe', () => {
  it('create an instance', () => {
    const pipe = new SortArrayByUpdatedOnPipe();
    expect(pipe).toBeTruthy();
  });
});
