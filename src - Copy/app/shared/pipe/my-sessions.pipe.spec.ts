import { MySessionsPipe } from './my-sessions.pipe';

describe('MySessionsPipe', () => {
  it('create an instance', () => {
    const pipe = new MySessionsPipe();
    expect(pipe).toBeTruthy();
  });
});
